# Experiments

Campaign configuration, manifests, run outputs, and logs.

| Directory | Contents |
|-----------|----------|
| `configs/` | Campaign templates (C0–C4) — rename to frozen ID after execution |
| `manifests/` | Run registry schema and frozen campaign manifests |
| `runs/` | Per-run artifacts (gitignored contents) |
| `logs/` | Execution logs (gitignored contents) |

**Authority:** [docs/study_design.md](../docs/study_design.md)
